# 🐳 Docker

## Build/run

```shell
# Build
docker build -t axum .

# Run
docker run --rm axum
```

## Images management

```shell
# List images
docker images

# Remove everything
docker image prune -a
```
